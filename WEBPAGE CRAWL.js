This is just the sample code.

#Mative motive is to find word and match with all links.
let vr=document.links;
let rv="dfrf"
for(var e=0;e<vr.length;e++)
{
if(vr[i].href.include("drdf")
{
console.log(vr[i].href)
}
}
